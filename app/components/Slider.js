var React = require('react');

var Slider = React.createClass({
  render: function() {
    return (
      <div className={"slider"}>
        <h1>I am a Slider.</h1>
      </div>
    )
  }
});

module.exports = Slider;